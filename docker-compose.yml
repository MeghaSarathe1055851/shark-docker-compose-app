version: '3'

services:
  app:
    build: ./app
    ports:
      - "80:3000"
    depends_on:
      - mongodb

  mongodb:
    image: mongo:6
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
